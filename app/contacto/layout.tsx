const layout = ({
  children
}: Readonly<{
  children: React.ReactNode
}>) => {
  return (
    <div>
      <title>La dulce tradicion | Contacto</title>
      {children}
    </div>
  )
}
export default layout
